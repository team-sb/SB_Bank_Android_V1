<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bank">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bank"
        android:usesCleartextTraffic="true">
        <activity android:name=".LoanCheckActivity"></activity>
        <activity android:name=".Account.activity.LoanActivity" />
        <activity android:name=".Account.activity.DepositActivity" />
        <activity android:name=".Main.activity.TransactionDetailsActivity" />
        <activity android:name=".Account.activity.CreateAccountActivity" />
        <activity android:name=".User.activity.MyPageActivity" />
        <activity android:name=".Admin.activity.AdminActivity" />
        <activity android:name=".Account.activity.SendActivity" />
        <activity android:name=".Account.activity.SendDataActivity" />
        <activity android:name=".Account.activity.WithdrawActivity" />
        <activity android:name=".Auth.activity.RegisterActivity" />
        <activity android:name=".Auth.activity.LoginActivity" />
        <activity android:name=".Account.activity.ApprovalNumberActivity" />
        <activity android:name=".Auth.activity.SecPasswordActiviity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main.activity.MainActivity" />
    </application>

</manifest>